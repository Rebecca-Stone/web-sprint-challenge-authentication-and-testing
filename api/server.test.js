// import the servers
// import supertest
// import db
// import restricted
// import other middleware

// beforeAll(async () => { });

// beforeEach(async () => { });

// afterAll(async () => { });

// Write your tests here
test("sanity", () => {
  expect(true).toBe(true);
});

// test 1 & 2
// ---------------------------------------------------------------
// test('[1] POST/api/auth/register -returns a new user', () => {});
// test('[2] POST/api/auth/register -the user is found within all users', () => {});

// test 3 & 4
// ---------------------------------------------------------------
// test('[3] POST/api/auth/login - logs an existing user in', () => {});
// test('[4] POST/api/auth/login - logged in users can see dad jokes', () => {});

// test 5 & 6
// ---------------------------------------------------------------
// test('[5] GET/api/jokes/ -jokes are visible to existing users', () => {});
// test('[6] GET/api/jokes/ -', () => {});
